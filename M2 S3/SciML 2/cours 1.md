这份总结专为考场复习设计，采用高度浓缩的模块化布局，涵盖了从逼近论基础到高级优化（NG、Greedy）的核心公式与结论。

---

## 📄 SciML 2 考前核心速记表 (A4版)

### 1. 逼近论基础 (Approximation Theory)

- **线性空间 $V_n$**: $u_\theta(\mathbf{x}) = \sum_{i=1}^n \theta_i \phi_i(\mathbf{x})$。
    
- **投影方程**: $\langle u_\theta - u, \psi_i \rangle_{L^2} = 0 \implies \mathbf{A}\theta = \mathbf{b}$。
    
    - **Galerkin (正交)**: $\psi_i = \phi_i$。矩阵 $A_{ij} = \langle \phi_i, \phi_j \rangle$ 对称正定，稳定性 $L=1$。
        
    - **Collocation (配置)**: $\psi_i = \delta_{\mathbf{x}_i}$。即 $u_\theta(\mathbf{x}_i) = u(\mathbf{x}_i)$。易于处理复杂几何，但矩阵可能病态。
        
- **非线性空间/流形**: 神经网络构成**子流形** $\mathcal{M}$。
    
    - **切空间 (Tangent Space)**: $T_\theta \mathcal{M} = \text{Span}\left( \frac{\partial u_\theta}{\partial \theta_1}, \dots, \frac{\partial u_\theta}{\partial \theta_n} \right)$。
        

---

### 2. PINNs 物理约束求解

- **损失函数**: $J(\theta) = \mathcal{L}_{res} + \lambda \mathcal{L}_{bc}$。
    
    - $\mathcal{L}_{res} = \sum |L(u_\theta(\mathbf{x}_i)) - f(\mathbf{x}_i)|^2$。
        
- **边界条件 (BC)**:
    
    - **弱约束**: 罚函数法。$\lambda$ 的选择关键（可用 Annealing 动态调整：$\lambda_k = \alpha \hat{\lambda}_k + (1-\alpha)\lambda_{k-1}$）。
        
    - **强约束**: 构造 $u_\theta(\mathbf{x}) = A(\mathbf{x})NN_\theta(\mathbf{x}) + B(\mathbf{x})$，其中 $A$ 在边界为 0。
        

---

### 3. 高级优化算法 (Advanced Optimization)

#### A. 自适应基与混合求解 (Adaptive Basis)

- **结构**: $u_\theta(\mathbf{x}) = \sum \alpha_l \phi_l(\mathbf{x}; \beta)$。
    
- **算法**: 交替进行 **梯度下降**（更新隐藏层参数 $\beta$ 以调整基函数）和 **最小二乘 LS**（直接计算输出层系数 $\alpha$）。
    
- **初始化**: 为避免 ReLU 死亡，使用 $k \mathbf{n} \cdot (\mathbf{x}-\mathbf{p})$ 初始化，确保切割面在区域内均匀分布。
    

#### B. 自然梯度法 (Natural Gradient, NG)

- **核心**: 在函数空间做最速下降，投影回参数空间。
    
- **NG 方程**: $\theta_{k+1} = \theta_k - \eta G^{-1}(\theta) \nabla_\theta J$。
    
- **Gram 矩阵 (Fisher)**: $G_{ij} = \int_\Omega \frac{\partial u_\theta}{\partial \theta_i} \frac{\partial u_\theta}{\partial \theta_j} d\mathbf{x}$。
    
- **PINNs 扩展**: $G_{ij} = \int_\Omega (L \frac{\partial u_\theta}{\partial \theta_i})(L \frac{\partial u_\theta}{\partial \theta_j}) d\mathbf{x}$。
    
- **计算技巧**: 经验自然梯度（Empirical NG）利用 Woodbury 恒等式在点数 $m < n$ 时加速：$(J^T J + \lambda I)^{-1} J^T = J^T (J J^T + \lambda I)^{-1}$。
    

#### C. 贪婪算法 (Greedy Methods)

- **基本思想**: 逐个增加基函数 $\phi_k \in \mathbb{D}$。
    
- **OGA (正交贪婪)**: 选定新基函数后，通过正交投影一次性优化所有 $\alpha_1, \dots, \alpha_k$。
    
- **Relaxed Greedy**: $u_k = (1-\beta_k)u_{k-1} + M\beta_k \phi_k$，收敛速度 $O(k^{-0.5})$，比普通贪婪更稳定。
    

---

### 4. 关键对比总结表

|**特性**|**PINNs**|**VPINNs**|**NG 优化**|**Greedy**|
|---|---|---|---|---|
|**投影类型**|配置法 (Collocation)|弱形式 (Petrov-Galerkin)|流形投影 (Riemannian)|字典学习|
|**核心优势**|简单、无网格|精度高、鲁棒性强|规避非凸性、收敛快|自动寻找最优基|
|**挑战**|局部最优、$\lambda$ 平衡|积分复杂、基函数选择|$G$ 矩阵求逆、显存占用|字典规模、病态矩阵|

---

### 💡 临考提醒 (Tips)

1. **Tangent Space** 决定了模型在当前参数下的逼近能力，若基函数线性相关，则 $G$ 矩阵不可逆。
    
2. **Adaptive Basis** 的优势在于将非线性问题部分“线性化”，LS 步能极大加速收敛。
    
3. **Stability**: Galerkin 方法的稳定性由空间 $V_n$ 保证，而 Collocation 严重依赖采样点分布（需 Theorem 保证解的唯一性）。
    

---

**需要我针对其中某个算法（比如自然梯度的矩阵推导过程）再补充一份简版公式证明吗？**